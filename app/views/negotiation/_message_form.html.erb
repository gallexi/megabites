<%= form_for(@message = Message.new, :url => url_for(:controller => 'negotiation', :action => 'new_message', :negotiation_id => @negotiation.id), remote: true) do |m| %>

		<%= m.text_area :body %>
		<%= m.submit "Send" %>

<% end %>